# workshop-node-aws

## Technologies used
    * AWS
    * Terraform 1.0
    * Javascript

## How to run?
1. Setup AWS CLI credentials
2. Go to `lambda/scripts` and run `build.sh`
3. Got to `terraform` and run:
    * `terraform init`
    * `terraform plan`
    * `terraform apply`